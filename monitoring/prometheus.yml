global:
  scrape_interval: 30s
  evaluation_interval: 30s
  external_labels:
    network: testnet

scrape_configs:
  - job_name: dcc-chain
    static_configs:
      - targets: ['127.0.0.1:9101']
    relabel_configs:
      - target_label: network
        replacement: testnet
