name: Check Plugins Dir
on:
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    environment: testnet
    steps:
      - name: Remove stale proto classes from plugin JARs and restart node
        env:
          SSH_KEY: ${{ secrets.TESTNET_DEPLOY_SSH_KEY }}
          SSH_HOST: ${{ secrets.TESTNET_DEPLOY_HOST }}
          SSH_USER: ${{ secrets.TESTNET_DEPLOY_USER }}
        run: |
          echo "$SSH_KEY" > /tmp/id_rsa && chmod 600 /tmp/id_rsa
          ssh -i /tmp/id_rsa -o StrictHostKeyChecking=no -o ConnectTimeout=15 \
            "${SSH_USER}@${SSH_HOST}" \
            'set +e
             python3 -c "
import zipfile,shutil
for jar in [\"/opt/dcc/plugins/testnet/dcc-grpc.jar\",\"/opt/dcc/plugins/testnet/dcc-ext.jar\"]:
    tmp=\"/tmp/fixed.jar\"
    removed=0
    with zipfile.ZipFile(jar,\"r\") as zin:
        with zipfile.ZipFile(tmp,\"w\",zipfile.ZIP_DEFLATED) as zout:
            for item in zin.infolist():
                n=item.filename
                if n.startswith(\"io/decentralchain/protobuf\") or n.startswith(\"scalapb/\") or n.startswith(\"io/decentralchain/events/\"):
                    removed+=1
                else:
                    zout.writestr(item,zin.read(item.filename))
    shutil.copyfile(jar,jar+\".bak\")
    shutil.move(tmp,jar)
    print(str(removed)+\" stale classes removed from \"+jar)
"
             echo "=== Restarting node ==="
             docker restart node-scala-testnet
             sleep 25
             docker ps --format "{{.Names}}\t{{.Status}}" | grep node-scala
             curl -s http://localhost:6869/node/version 2>/dev/null || echo "(starting)"
             sleep 15
             echo "=== Port 6881 check ==="
             ss -tlnp 2>/dev/null | grep 6881 || echo "port 6881 not yet open"'
          rm -f /tmp/id_rsa
