name: Get Node Addresses
on:
  workflow_dispatch:
jobs:
  addrs:
    runs-on: ubuntu-latest
    steps:
      - name: Download kubeconfig
        env:
          LINODE_TOKEN: ${{ secrets.LINODE_TOKEN }}
        run: |
          RAW=$(curl -s -H "Authorization: Bearer $LINODE_TOKEN" \
            -X GET "https://api.linode.com/v4/lke/clusters/615553/kubeconfig" | jq -r '.kubeconfig')
          echo "$RAW" | python3 -c 'import base64,sys; sys.stdout.buffer.write(base64.b64decode(sys.stdin.read().strip() + "=="))' > /tmp/kubeconfig
          chmod 600 /tmp/kubeconfig
          echo "KUBECONFIG=/tmp/kubeconfig" >> $GITHUB_ENV
      - uses: azure/setup-kubectl@v4
      - name: Get wallet addresses via port-forward
        run: |
          kubectl get pods -n dcc
          echo "=== GEN-0 ADDRESS ==="
          kubectl port-forward -n dcc dcc-gen-0-0 16869:6869 &
          PF0=$!
          sleep 5
          curl -s http://127.0.0.1:16869/addresses || echo "gen-0 not responding"
          kill $PF0 2>/dev/null; true
          echo ""
          echo "=== GEN-1 ADDRESS ==="
          kubectl port-forward -n dcc dcc-gen-1-0 16870:6870 &
          PF1=$!
          sleep 5
          curl -s http://127.0.0.1:16870/addresses || echo "gen-1 not responding"
          kill $PF1 2>/dev/null; true
