name: Dispatch Test
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'test'
        required: false
        default: 'manual'
  workflow_call:
jobs:
  test:
    runs-on: ubuntu-latest
    environment: testnet
    steps:
      - run: echo "dispatch works"
