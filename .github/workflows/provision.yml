# ──────────────────────────────────────────────────────────────────────────────
# OpenTofu: Provision / update Linode servers
#
# Triggered manually via workflow_dispatch with a target (plan | apply | destroy).
# Uses OpenTofu 1.11.0 (stable) with the Linode provider v3.12.0.
#
# State backend: Linode Object Storage (S3-compatible).
# Create the state bucket before first run:
#   linode-cli obj mb dcc-tofu-state --cluster us-east-1
#
# Required secrets (infra repo level, not environment-scoped):
#   LINODE_TOKEN          — Linode API token (read/write)
#   LINODE_OBJ_ACCESS_KEY — Object Storage access key (for state backend)
#   LINODE_OBJ_SECRET_KEY — Object Storage secret key (for state backend)
# ──────────────────────────────────────────────────────────────────────────────

name: Provision Infrastructure (OpenTofu)

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'OpenTofu action'
        required: true
        default: 'plan'
        type: choice
        options:
          - plan
          - apply
          - destroy
      network:
        description: 'Target network workspace'
        required: true
        default: 'mainnet'
        type: choice
        options:
          - mainnet
          - stagenet
          - testnet

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  tofu:
    name: OpenTofu ${{ inputs.action }} (${{ inputs.network }})
    runs-on: ubuntu-latest
    timeout-minutes: 30

    defaults:
      run:
        working-directory: terraform/

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up OpenTofu
        uses: opentofu/setup-opentofu@fc711fa910b93cba0f3fbecaafc9f42fd0c411cb # v2.0.0
        with:
          tofu_version: '1.11.0'

      - name: OpenTofu Init
        run: |
          tofu init \
            -backend-config="access_key=${{ secrets.LINODE_OBJ_ACCESS_KEY }}" \
            -backend-config="secret_key=${{ secrets.LINODE_OBJ_SECRET_KEY }}"
        env:
          LINODE_TOKEN: ${{ secrets.LINODE_TOKEN }}

      - name: Select / create workspace
        run: tofu workspace select -or-create "${{ inputs.network }}"
        env:
          LINODE_TOKEN: ${{ secrets.LINODE_TOKEN }}

      - name: OpenTofu Plan
        if: inputs.action == 'plan' || inputs.action == 'apply'
        run: tofu plan -out=tfplan -input=false
        env:
          LINODE_TOKEN: ${{ secrets.LINODE_TOKEN }}

      - name: OpenTofu Apply
        if: inputs.action == 'apply'
        run: tofu apply -auto-approve tfplan
        env:
          LINODE_TOKEN: ${{ secrets.LINODE_TOKEN }}

      - name: OpenTofu Destroy
        if: inputs.action == 'destroy'
        run: tofu destroy -auto-approve -input=false
        env:
          LINODE_TOKEN: ${{ secrets.LINODE_TOKEN }}
