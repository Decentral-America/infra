name: Commit Generators to HotStuff (T2 Activation)
# Submits CommitToGenerationTransaction (type 19) for all 3 generators.
# The node's /transactions/sign endpoint auto-derives BLS keys from the
# generator's Ed25519 private key and computes the Proof of Possession.
# Run this once per generation period (every 100 blocks) to keep T2 active.
#
# This previously had NO automated trigger at all -- only workflow_dispatch/
# workflow_call, meaning it only ever ran when someone manually clicked it
# during an active debugging session. At ~30.3s/block, 100 blocks is ~50
# minutes; with nobody manually re-triggering it, the committee silently
# expired and both T0 (lag pinned at the max-rollback ceiling) and T2
# (hotStuffFinalizedHeight stalled) broke again 15 days after the last
# manual run (confirmed live 2026-07-22: last successful run was 2026-07-07).
# The 20-minute cron gives >2x safety margin inside each ~50-minute period.
#
# 2026-08-31 addendum: confirmed live that GitHub's `schedule` trigger is
# genuinely best-effort and can go silent for hours during periods of high
# Actions load across the org/repo -- both this workflow's cron AND
# auto-commit-generators.yml's went dark for 3+ hours during a single heavy
# CI day, independently corroborated by peer-watchdog.yml (a totally
# unrelated */5 cron) also going dark for 5.5 hours in the exact same
# window. This isn't fixable by tuning the cron expression -- GitHub does
# not guarantee scheduled dispatch under load, full stop. Added `push` on
# main as a second, STRUCTURALLY INDEPENDENT trigger path (tied to the push
# webhook, not the schedule dispatcher) so a schedule-specific outage isn't
# the only thing standing between the committee and going empty -- this repo's
# main branch gets pushed to often enough on its own to provide real, frequent
# coverage. Safe to over-trigger: each run is idempotent ("already committed"
# is the normal no-op outcome for a generator that doesn't need to re-commit).
on:
  schedule:
    - cron: '*/20 * * * *'
  push:
    branches: [main]
  workflow_call:
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  commit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: testnet
    steps:
      - name: Install kubectl
        uses: azure/setup-kubectl@829323503d1be3d00ca8346e5391ca0b07a9ab0d  # v5.1.0

      - name: Get LKE kubeconfig
        env:
          LINODE_TOKEN: ${{ secrets.LINODE_TOKEN }}
        run: |
          CLUSTER_ID=$(curl -s -H "Authorization: Bearer $LINODE_TOKEN" \
            "https://api.linode.com/v4/lke/clusters" | \
            jq -r '.data[] | select(.label | test("testnet")) | .id' | head -1)
          RAW=$(curl -s -H "Authorization: Bearer $LINODE_TOKEN" \
            "https://api.linode.com/v4/lke/clusters/${CLUSTER_ID}/kubeconfig" | \
            jq -r '.kubeconfig')
          echo "$RAW" | base64 -d > /tmp/kubeconfig
          chmod 600 /tmp/kubeconfig
          echo "KUBECONFIG=/tmp/kubeconfig" >> $GITHUB_ENV
          echo "LKE cluster $CLUSTER_ID kubeconfig downloaded"

      - name: Verify cluster connectivity
        run: kubectl cluster-info --request-timeout=10s

      - name: Commit main node generator
        env:
          SSH_KEY:      ${{ secrets.TESTNET_DEPLOY_SSH_KEY }}
          SSH_HOST:     ${{ secrets.TESTNET_DEPLOY_HOST }}
          SSH_USER:     ${{ secrets.TESTNET_DEPLOY_USER }}
          MAIN_API_KEY: ${{ secrets.MAIN_NODE_REST_API_KEY }}
        run: |
          set -euo pipefail
          echo "$SSH_KEY" > /tmp/id_rsa && chmod 600 /tmp/id_rsa
          # Write key to temp file to avoid SSH quoting issues with Base64 special chars
          echo "${MAIN_API_KEY}" > /tmp/.main_key
          scp -i /tmp/id_rsa -o StrictHostKeyChecking=no /tmp/.main_key "${SSH_USER}@${SSH_HOST}:/tmp/.main_key" 2>/dev/null
          rm -f /tmp/.main_key
          ssh -i /tmp/id_rsa -o StrictHostKeyChecking=no -o ConnectTimeout=15 \
            "${SSH_USER}@${SSH_HOST}" \
            'set -euo pipefail
             API_KEY=$(cat /tmp/.main_key 2>/dev/null | tr -d "\n")
             rm -f /tmp/.main_key
             ADDR="31RPEKcz71a3hdxt8z7qLhTpRMuRV2kUyr6"
             echo "=== Signing CommitToGeneration for main node ==="
             SIGNED=$(curl -s -X POST http://localhost:6869/transactions/sign \
               -H "X-API-Key: $API_KEY" \
               -H "Content-Type: application/json" \
               -d "{\"type\":19,\"sender\":\"$ADDR\"}")
             echo "Signed response: $SIGNED" | head -c 300
             echo ""
             if echo "$SIGNED" | grep -q "\"id\""; then
               echo "=== Broadcasting ==="
               RESULT=$(curl -s -X POST http://localhost:6869/transactions/broadcast \
                 -H "Content-Type: application/json" \
                 -d "$SIGNED")
               echo "Broadcast result: $RESULT" | head -c 300
               echo ""
               if echo "$RESULT" | grep -q "\"id\""; then
                 echo "Main: broadcast OK"
               elif echo "$RESULT" | grep -qi "is already committed"; then
                 echo "Main: already committed (no-op)"
               else
                 echo "Main: broadcast FAILED"
                 exit 1
               fi
             else
               echo "Main: sign FAILED"
               exit 1
             fi'
          rm -f /tmp/id_rsa

      - name: Commit gen-0 generator (port-forward)
        env:
          API_KEY: ${{ secrets.GEN_0_NODE_REST_API_KEY }}
        run: |
          set -euo pipefail
          ADDR="31PmKNdHAU5sZbtg8TrzKh8WfE7E8xBc9WD"
          echo "=== Port-forward gen-0 REST API (localhost:16869) ==="
          kubectl port-forward pod/dcc-gen-0-0 -n dcc 16869:6869 &
          PF_PID=$!
          trap 'kill $PF_PID 2>/dev/null || true' EXIT
          sleep 5
          echo "=== Signing CommitToGeneration for gen-0 ==="
          SIGNED=$(curl -s -X POST http://localhost:16869/transactions/sign \
            -H "X-API-Key: $API_KEY" \
            -H "Content-Type: application/json" \
            -d "{\"type\":19,\"sender\":\"$ADDR\"}")
          echo "Signed response: $SIGNED" | head -c 300
          echo ""
          if echo "$SIGNED" | grep -q '"id"'; then
            # gen-0's connection to the main node has been observed to be
            # genuinely intermittent (probabilistic, not on a fixed
            # schedule) -- it briefly appears in peers/connected and then
            # silently drops with no logged reason. A 6-attempt/2-minute
            # retry window sometimes still misses every connected gap in a
            # single run, so widen it: 15 attempts over ~5 minutes gives
            # meaningfully more chances to land inside a connected window
            # without materially lengthening the overall workflow.
            #
            # break ONLY on: (a) success ("id" in response) or (b) the
            # specific already-committed error. Retry on "not enough
            # connections" or a curl network failure (empty RESULT). Any
            # OTHER error is unexpected -- fail loudly instead of silently
            # swallowing it.
            for attempt in $(seq 1 15); do
              echo "=== Broadcasting (attempt ${attempt}/15) ==="
              set +e
              RESULT=$(curl -s -X POST http://localhost:16869/transactions/broadcast \
                -H "Content-Type: application/json" \
                -d "$SIGNED")
              CURL_RC=$?
              set -e
              echo "Broadcast result: $RESULT" | head -c 300
              echo ""
              if [ "$CURL_RC" -ne 0 ] || [ -z "$RESULT" ]; then
                echo "gen-0: curl network error (rc=$CURL_RC), retrying..."
              elif echo "$RESULT" | grep -q '"id"'; then
                break
              elif echo "$RESULT" | grep -qi "is already committed"; then
                break
              elif echo "$RESULT" | grep -q "not enough connections"; then
                : # retry
              else
                echo "gen-0: broadcast FAILED (unexpected error)"
                exit 1
              fi
              [ "$attempt" -lt 15 ] && sleep 20
            done
            if echo "$RESULT" | grep -q '"id"'; then
              TXID=$(echo "$RESULT" | python3 -c "import json,sys; print(json.load(sys.stdin)[\"id\"])")
              echo "gen-0: broadcast OK, id=$TXID (attempt ${attempt}/15)"
              echo "GEN0_TXID=$TXID" >> "$GITHUB_ENV"
            elif echo "$RESULT" | grep -qi "is already committed"; then
              echo "gen-0: already committed (no-op, attempt ${attempt}/15)"
            else
              echo "gen-0: exhausted retries without success"
              exit 1
            fi
          else
            echo "gen-0: sign FAILED"
            exit 1
          fi

      - name: Commit gen-1 generator (port-forward)
        env:
          API_KEY: ${{ secrets.GEN_1_NODE_REST_API_KEY }}
        run: |
          set -euo pipefail
          ADDR="31dLhqhGoGVhtkf5msWFmgZn1ErrVR6b9qV"
          echo "=== Port-forward gen-1 REST API (localhost:16870 -> pod:6870) ==="
          kubectl port-forward pod/dcc-gen-1-0 -n dcc 16870:6870 &
          PF_PID=$!
          trap 'kill $PF_PID 2>/dev/null || true' EXIT
          sleep 5
          echo "=== Signing CommitToGeneration for gen-1 ==="
          SIGNED=$(curl -s -X POST http://localhost:16870/transactions/sign \
            -H "X-API-Key: $API_KEY" \
            -H "Content-Type: application/json" \
            -d "{\"type\":19,\"sender\":\"$ADDR\"}")
          echo "Signed response: $SIGNED" | head -c 300
          echo ""
          if echo "$SIGNED" | grep -q '"id"'; then
            for attempt in $(seq 1 15); do
              echo "=== Broadcasting (attempt ${attempt}/15) ==="
              set +e
              RESULT=$(curl -s -X POST http://localhost:16870/transactions/broadcast \
                -H "Content-Type: application/json" \
                -d "$SIGNED")
              CURL_RC=$?
              set -e
              echo "Broadcast result: $RESULT" | head -c 300
              echo ""
              if [ "$CURL_RC" -ne 0 ] || [ -z "$RESULT" ]; then
                echo "gen-1: curl network error (rc=$CURL_RC), retrying..."
              elif echo "$RESULT" | grep -q '"id"'; then
                break
              elif echo "$RESULT" | grep -qi "is already committed"; then
                break
              elif echo "$RESULT" | grep -q "not enough connections"; then
                : # retry
              else
                echo "gen-1: broadcast FAILED (unexpected error)"
                exit 1
              fi
              [ "$attempt" -lt 15 ] && sleep 20
            done
            if echo "$RESULT" | grep -q '"id"'; then
              TXID=$(echo "$RESULT" | python3 -c "import json,sys; print(json.load(sys.stdin)[\"id\"])")
              echo "gen-1: broadcast OK, id=$TXID (attempt ${attempt}/15)"
              echo "GEN1_TXID=$TXID" >> "$GITHUB_ENV"
            elif echo "$RESULT" | grep -qi "is already committed"; then
              echo "gen-1: already committed (no-op, attempt ${attempt}/15)"
            else
              echo "gen-1: exhausted retries without success"
              exit 1
            fi
          else
            echo "gen-1: sign FAILED"
            exit 1
          fi

      - name: Verify generators committed
        env:
          SSH_KEY:  ${{ secrets.TESTNET_DEPLOY_SSH_KEY }}
          SSH_HOST: ${{ secrets.TESTNET_DEPLOY_HOST }}
          SSH_USER: ${{ secrets.TESTNET_DEPLOY_USER }}
        run: |
          echo "$SSH_KEY" > /tmp/id_rsa && chmod 600 /tmp/id_rsa
          sleep 15
          ssh -i /tmp/id_rsa -o StrictHostKeyChecking=no -o ConnectTimeout=15 \
            "${SSH_USER}@${SSH_HOST}" \
            'H=$(curl -s http://localhost:6869/blocks/height | python3 -c "import json,sys; print(json.load(sys.stdin)[\"height\"])" 2>/dev/null)
             F=$(curl -s http://localhost:6869/blocks/height/finalized | python3 -c "import json,sys; print(json.load(sys.stdin)[\"height\"])" 2>/dev/null)
             echo "Height: $H Finalized: $F Lag: $(( ${H:-0} - ${F:-0} ))"'
          rm -f /tmp/id_rsa

      # Real post-commit verification: previously nothing checked that a broadcast
      # commit actually resulted in a non-empty NEXT-period committee. This step
      # polls each freshly-broadcast tx for confirmation, then queries
      # GET /generators/at/{nextPeriodStart} on the main node's public HTTPS
      # endpoint and requires BOTH HTTP 200 AND a non-empty JSON array body.
      #
      # NOTE: out of range, this endpoint returns HTTP 404 with body "[]" --
      # a naive `jq length` on that body reads as "0 generators", which is WRONG
      # (it means "no data for that height", not "empty committee"). Checking
      # HTTP status is mandatory, not optional.
      - name: Verify next-period committee
        run: |
          set -euo pipefail
          NODE_URL="https://testnet-node.decentralchain.io"

          for label in GEN0 GEN1; do
            eval "TXID=\${${label}_TXID:-}"
            if [ -z "$TXID" ]; then
              echo "$label: no fresh tx this run (already committed) — skipping confirmation poll"
              continue
            fi
            CONFIRMED=0
            for attempt in $(seq 1 15); do
              INFO=$(curl -s "${NODE_URL}/transactions/info/${TXID}" || true)
              if echo "$INFO" | grep -q '"height"'; then
                CONFIRMED=1
                echo "$label: tx $TXID confirmed (attempt ${attempt}/15)"
                break
              fi
              sleep 10
            done
            if [ "$CONFIRMED" -ne 1 ]; then
              echo "$label: tx $TXID did NOT confirm after 15 attempts (~2.5min)"
              exit 1
            fi
          done

          # Mirrors the node's GenerationPeriod.next.start arithmetic for the
          # steady-state (non-zero period) case: testnet generation-period-length=100.
          HEIGHT_JSON=$(curl -s "${NODE_URL}/blocks/height")
          CURR_HEIGHT=$(echo "$HEIGHT_JSON" | python3 -c "import json,sys; print(json.load(sys.stdin)['height'])")
          GEN_PERIOD_LEN=100
          NEXT_PERIOD_START=$(( ((CURR_HEIGHT / GEN_PERIOD_LEN) + 1) * GEN_PERIOD_LEN + 1 ))

          HTTP_CODE=$(curl -s -o /tmp/generators_body -w '%{http_code}' \
            "${NODE_URL}/generators/at/${NEXT_PERIOD_START}")

          if [ "$HTTP_CODE" != "200" ]; then
            echo "Committee verification FAILED: GET /generators/at/${NEXT_PERIOD_START} returned HTTP ${HTTP_CODE} (expected 200). Body: $(cat /tmp/generators_body | head -c 300)"
            exit 1
          fi

          COUNT=$(jq 'length' /tmp/generators_body 2>/dev/null || echo 0)
          if [ "$COUNT" -le 0 ]; then
            echo "Committee verification FAILED: HTTP 200 but next-period committee at height ${NEXT_PERIOD_START} is EMPTY ($(cat /tmp/generators_body))"
            exit 1
          fi

          echo "Committee verification OK: next period (start ${NEXT_PERIOD_START}) has ${COUNT} committed generator(s)"
